# Frontend Environment Variables Example
# Set these in Netlify Dashboard > Site Settings > Environment Variables

# API Configuration
# Replace with your Railway backend URL
NEXT_PUBLIC_API_URL=https://your-app.up.railway.app/api

# Optional: Force secure cookies (for production)
NEXT_PUBLIC_FORCE_SECURE_COOKIES=true

# Optional: Enable credential sending (for cookie auth)
NEXT_PUBLIC_SEND_CREDENTIALS=true

# CSRF config (used when NEXT_PUBLIC_SEND_CREDENTIALS=true)
NEXT_PUBLIC_CSRF_HEADER_NAME=X-CSRF-Token
NEXT_PUBLIC_CSRF_COOKIE_NAME=csrf_token

# Public site URL used as fallback by sitemap/robots when backend settings are unavailable
NEXT_PUBLIC_SITE_URL=https://your-frontend.netlify.app

# Build-time fetch safety (highly recommended on Netlify to avoid timeouts if API is offline)
# Set to 'true' to skip remote fetches during `next build`
DISABLE_BUILD_TIME_SETTINGS_FETCH=true

# Timeout in ms for server-side fetches during build/SSR
SETTINGS_FETCH_TIMEOUT_MS=4000
